import React, { Component } from "react";
import { View, Text } from "react-native";

export default class RegisterScreenPassword extends Component {

    render(){
        return(
            <View><Text>RegisterScreenPassword Screen</Text></View>
        );
    }
}